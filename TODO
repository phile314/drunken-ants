Try to write parsers (with any parsing lib) for the datatype defined Ast.hs

Add different newlines for different OSes

I don't think we should track Tokens.hs (it's just generated code...) (makefile for this kind of stuff...)
